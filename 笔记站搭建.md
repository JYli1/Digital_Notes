太棒了！恭喜你成功搭建了自己的“数字花园”。

为了防止你以后忘记，我把刚才我们踩过的坑、验证过的最佳路径，整理成了一份**《Obsidian + Quartz + Netlify 终极部署手册》**。

你可以把这份教程保存在你的 Obsidian 里，下次如果换电脑或者忘了怎么操作，照着做就行。

---

# 📚 Obsidian 数字花园部署教程 (Quartz 版)

## 架构核心
*   **私有库 (本地):** 你平时记笔记的地方 (乱糟糟也没事)。
*   **公开库 (Quartz):** 专门用来展示的“样板房”，只有你复制进去的内容会被发布。
*   **服务器 (Netlify):** 负责把公开库的内容变成网页。

---

## 第一阶段：环境搭建 (只需做一次)

### 1. 准备“样板房” (GitHub)
1.  登录 GitHub，访问模板仓库：[jackyzha0/quartz](https://github.com/jackyzha0/quartz)。
2.  点击右上角 **Use this template** -> **Create a new repository**。
3.  **Repository name:** 起个名字（如 `my-digital-garden`），选 **Public**。
4.  把这个新仓库 **Clone (克隆/下载)** 到你电脑本地。

### 2. 接通“水电” (Netlify)
1.  注册/登录 [Netlify](https://www.netlify.com/)。
2.  点击 **Add new site** -> **Import an existing project** -> **GitHub**。
3.  选择你刚才建的那个仓库。
4.  **填写构建配置 (关键，填错就 404):**
    *   **Branch:** `v4`
    *   **Base directory:** (留空)
    *   **Build command:** `npx quartz build`
    *   **Publish directory:** `public`
5.  点击 **Deploy**。

---

## 第二阶段：日常发布流程 (每次更新做这个)

### 1. 本地操作 (搬运)
打开你的电脑文件管理器，同时打开**私有库文件夹**和**Quartz 文件夹**。

1.  **复制笔记:** 把想发布的 `.md` 文件，从私有库复制到 Quartz 文件夹下的 **`content`** 目录中。
2.  **复制图片:** 把笔记里用到的图片，也复制到 `content` 目录下的任意位置（建议建一个 `content/assets` 统一存放）。
3.  **检查首页:** 确保 `content` 目录下有一个文件叫 **`index.md`** (全小写)，这是你的网站封面。

### 2. 图片修正 (最关键的一步！)
为了防止图片不显示，**必须**遵守以下两条铁律：

*   **铁律一：改用双链语法**
    *   不要用：`![](assets/图片.png)` (相对路径容易挂)
    *   **一定要用：`![[图片名.png]]`** (Quartz 会自动全库搜索图片)
*   **铁律二：拒绝空格**
    *   文件夹命名**不要有空格** (如 `Day 1` 改为 `Day1`)。
    *   图片文件名最好也不要有空格。

### 3. 推送上线 (Terminal)
在你的 Quartz 文件夹下右键打开终端 (Terminal/Git Bash)，依次输入以下三行命令：

```bash
# 1. 把所有变动放入暂存区
git add .

# 2. 确认提交 (引号里写备注)
git commit -m "update blog"

# 3. 推送到 GitHub (v4 是分支名)
git push origin v4
```

---

## 🛠️ 常见故障排查 (Troubleshooting)

### Q1: 打开网站显示 404 Not Found？
*   **原因 1:** `content` 文件夹里没有 `index.md`。
*   **原因 2:** `index.md` 文件名里有大写 (如 `Index.md`)。
*   **原因 3:** Netlify 构建失败。去 Netlify 官网 -> Deploys -> 点击 Failed 查看报错日志。

### Q2: 笔记里的图片裂了/不显示？
*   **检查:** 你是不是忘记把图片文件复制到 Quartz 文件夹里了？
*   **检查:** 路径里是不是有空格？(把 `Day 1` 改成 `Day1`)
*   **大招:** 把引用格式改成 `![[图片.png]]`，通常能解决 99% 的问题。

### Q3: Git 推送报错 `rejected`？
*   **原因:** 线上版本比你本地新。
*   **解决:** 强制推送覆盖线上：`git push -f origin v4`。

---

### 💡 进阶提示 (未来如果有空折腾)
如果你以后觉得手动复制太麻烦，可以在 Obsidian 里安装 **"GitHub Publisher"** 插件。
*   **配置:** Repo Name 填你的 Quartz 仓库，Branch 填 `v4`，Root Folder 填 `content`。
*   **效果:** 在笔记里点一下命令，插件自动帮你把文字和图片搬运并上传，省去手动复制的步骤。